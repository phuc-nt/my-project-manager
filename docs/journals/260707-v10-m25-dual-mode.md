# v10 M25 — Dual-mode low/high tech (2026-07-07)

Frontend + 1 field backend additive. Milestone lớn nhất v10.

## Đã làm

- **Toggle "Chế độ nâng cao"** (`ui-mode-context.tsx`, `localStorage['ui-mode']`, default `low`). Low = nav 4 mục CEO-first (không đổi). High = thêm hàng nav thứ 2 với 7 view kỹ thuật (Tổng quan/Dòng thời gian/Chi phí/Bộ nhớ/Guardrail/Cấu hình/Chạy tay). Bật trong Cài đặt → Chế độ hiển thị. Mode = view-layer THUẦN — không đổi API/permission (route nâng cao vẫn vào được bằng URL ở low mode; auth + gateway mới là ranh giới thật).
- **i18n 7 view + 5 component** ra tiếng Việt (Overview/Timeline/Cost/MemoryAuto/Guardrail/Config + RunList/AuditTable/FactsList/ConfigEditor/PendingProposals). Dùng `labels.ts` sẵn (KIND_LABEL/RUN_STATUS_LABEL/VERDICT_LABEL/formatDateTime) + thêm `AUDIENCE_LABEL`. E2E xác nhận 0 chuỗi EN rò rỉ.
- **F4 (red-team): Trigger đọc kind per-agent**. Trước: hardcode 4 kind PM → agent hr/admin bị offer sai. Nay: field additive `report_kinds` vào `/api/agents` (`agent_views._report_kinds_for_domain`, nguồn từ pack của agent). Trigger render kind của chính pack đó, nhãn VN. Live: `default`→pm kinds, `admin`→admin-pack kinds (audit-digest/cost-rollup...).
- **F5 (red-team): SSE phân biệt "xong" vs "mất kết nối"**. `use-sse.ts` cũ map `onerror → done=true` → rớt mạng hiện "(xong)" (nói dối). Nay thêm cờ `errored` + guard `terminated` (terminal frame → không phải lỗi); Trigger hiện "(mất kết nối)" + cảnh báo.
- **Chart theme-aware**: `key={resolvedTheme}` remount CostChart/VerdictChart khi đổi theme (đọc lại token màu). Team high-mode thêm thanh ngân sách (pure CSS, ok→warn→over theo tỉ lệ).

## Verified

- Web vitest **96/96** (thêm ui-mode 3, Trigger 2) + tsc + build clean; backend **1206** (1205 cũ + 1 report_kinds); ruff + oxlint clean.
- **E2E live 29/29** (puppeteer, server thật, 4 tổ hợp low/high × light/dark): low ẩn nav nâng cao / high hiện 7 mục VN / toggle round-trip; 7 view VN 0 English leak; Trigger kind VN per-agent; chart render cả 4 combo 0 console error.
- Code-review DONE_WITH_CONCERNS → vá hết:
  - **M1 (MAJOR)**: `list_agents()` load pack per-agent (không cache) → N agent = N lần exec module pack. Vá: memo per-domain trong view (≤3 domain → ≤3 load bất kể N).
  - **m2 (MINOR)**: broad `except` che pack hỏng → thêm `logger.warning` trước khi degrade [].
  - **m1 (MINOR)**: thanh ngân sách bị ép mỏng ở mobile card-list → bọc `.budget-cell` flex-column, bar xuống dòng dưới.

## Bài học

- **1 hàm/field cho 2 phạm vi = bug tiềm ẩn (F4)**: `all_report_kinds()` là UNION toàn pack (validate typo), nhưng Trigger cần kind của RIÊNG pack agent — union sai cho form. Đọc đúng phạm vi per-agent mới khớp.
- **Vòng lặp gọi loader không cache = nhân chi phí (M1)**: pack load re-exec module; đặt trong `for agent` là nhân N. Memo per-distinct-key là fix rẻ, đúng chỗ (view), không đụng contract.
- **"done" ≠ "ended well" (F5)**: gộp lỗi-kết-nối vào cờ done làm UI nói dối. Tách "kết thúc bình thường" (có terminal frame) khỏi "rớt giữa chừng".

## Unresolved / next (M26)

1. Team high-density chỉ có thanh ngân sách (data model chưa expose model/run-series — không thêm backend để giữ additive-minimal; 7 view nâng cao đã là giá trị high-mode chính).
2. M26: gia cố `deploy/install.sh` (idempotent, restart-only-on-change F6) + view "Sức khỏe hệ thống".
