<!-- Agent-maintained memory for the `default` agent. Empty by default.
     Read verbatim into report context (M1: read-only; agent self-writes land in M2-P8).
     Lines accumulate notable cross-report facts, e.g. "Sprint 4 slipped due to X". -->
