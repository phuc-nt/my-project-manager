<!-- Project context for the `default` agent. Empty ⇒ no extra context added.
     Add team / milestone / business conventions here (e.g. "label p0 = blocker")
     to feed the analyze/compose step. Kept separate from persona (SOUL.md). -->
