<!-- Persona for the `default` agent. Empty ⇒ the v1 system prompt is used unchanged.
     Add team tone / behavior rules here to prepend them to the system prompt.
     NOTE: persona prepends; it does NOT override the external PII-sanitization. -->
