/*
 * PLACEHOLDER — htmx 2.x is not bundled in this commit.
 *
 * The dashboard templates reference /static/htmx.min.js (hx-get / hx-post). The
 * mount + tests work with this placeholder (tests only string-check the reference),
 * but the live UI needs the REAL htmx before use. Before running the dashboard for
 * real, replace this file with htmx 2.x:
 *
 *     htmx.org@2 dist/htmx.min.js  (~50 KB)  →  src/server/static/htmx.min.js
 *
 * (fetched once from the htmx release; an offline build environment blocked the
 * automated fetch, so it is left as an operator drop-in.)
 */
